# Agent-SkillOpt 操作契约

## 目的

本技能文件描述 Agent-SkillOpt 的安全使用边界，供能够执行本地命令的 Agent 宿主
参考。它不是关于所有 Agent、所有模型或所有工具宿主的通用兼容性声明。

## 前置条件

- Python 3.10 或更高版本。
- 用户自己管理的 Microsoft SkillOpt 本地检出。
- 与配置一致的本地数据路径。
- 只有在获得用户对模型、预算、并发和数据范围的明确授权后，才允许网络调用。

## 默认流程

1. 运行 doctor，收集本地配置、数据和上游特征诊断。
2. 使用 run --dry-run 生成命令；不得在此阶段创建子进程或网络调用。
3. 告知用户潜在的费用与数据外发范围。
4. 只有用户显式授权且使用 allow-network 时，才启动上游执行。
5. 使用 report 输出 manifest 和已提供指标的证据，不推断缺失分数。

## 保密规则

密钥仅可作为环境变量的值提供给受控子进程。任何面向用户的输出、日志、报告、
配置或版本控制内容都只能引用环境变量名称，绝不可引用其值。

## 兼容性声明

已验证范围以 docs/compatibility.md 为准。未知 Agent 宿主只能按本契约尝试，结果
必须标注为未验证，不能升级为通用兼容性承诺。
